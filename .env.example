# Optional paths. Provider and Telegram credentials belong in the YAML config.
CONFIG_PATH=config.yaml
CONTACTS_PATH=contacts.json
DB_PATH=./data/cahciua.db # Drizzle CLI only; application DB path comes from YAML.

# Use production for JSON logs; all other values use pretty development logs.
NODE_ENV=development
